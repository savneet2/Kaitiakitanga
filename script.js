
document.addEventListener("DOMContentLoaded", function() {
    // Select the heading element
    var heading = document.getElementById("heading");

    // Change the text color to navy blue
    heading.style.color = "navy";
});
/* styles.css */
body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
}

#heading {
    color: navy;
    font-weight: bold;
    text-align: center;
    padding: 20px 0;
}

#content {
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
    line-height: 1.6;
}
// script.js
document.addEventListener("DOMContentLoaded", function() {
    // Select the heading element
    var heading = document.getElementById("heading");

    // Select the content section
    var content = document.getElementById("content");

    // Add click event listener to the heading
    heading.addEventListener("click", function() {
        // Toggle the visibility of the content section
        if (content.style.display === "none") {
            content.style.display = "block";
        } else {
            content.style.display = "none";
        }
    });
});
// script.js
document.addEventListener("DOMContentLoaded", function() {
    // Select all section titles
    var sectionTitles = document.querySelectorAll("h2");

    // Add click event listener to each section
2
    // script.js
    document.addEventListener("DOMContentLoaded", function() {
        // Select all section titles
        var sectionTitles = document.querySelectorAll("h2");

        // Add click event listener to each section title
        sectionTitles.forEach(function(title) {
            title.addEventListener("click", function() {
                // Toggle the display of the next sibling element (content under each section title)
                var nextElement = title.nextElementSibling;
                while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                    nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                    nextElement = nextElement.nextElementSibling;
                }
            });
        });
    });

                          // script.js
                          document.addEventListener("DOMContentLoaded", function() {
                              // Select all section titles
                              var sectionTitles = document.querySelectorAll("h2");

                              // Add click event listener to each section title
                              sectionTitles.forEach(function(title) {
                                  title.addEventListener("click", function() {
                                      // Toggle the display of the next sibling element (content under each section title)
                                      var nextElement = title.nextElementSibling;
                                      while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                          nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                          nextElement = nextElement.nextElementSibling;
                                      }
                                  });
                              });
                          });

    // script.js
    document.addEventListener("DOMContentLoaded", function() {
        // Select all section titles
        var sectionTitles = document.querySelectorAll("h2");

        // Add click event listener to each section title
        sectionTitles.forEach(function(title) {
            title.addEventListener("click", function() {
                // Toggle the display of the next sibling element (content under each section title)
                var nextElement = title.nextElementSibling;
                while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                    nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                    nextElement = nextElement.nextElementSibling;
                }
            });
        });
    });
    // script.js
    document.addEventListener("DOMContentLoaded", function() {
        // Select all section titles
        var sectionTitles = document.querySelectorAll("h2");

        // Add click event listener to each section title
        sectionTitles.forEach(function(title) {
            title.addEventListener("click", function() {
                // Toggle the display of the next sibling element (content under each section title)
                var nextElement = title.nextElementSibling;
                while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                    nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                    nextElement = nextElement.nextElementSibling;
                }
            });
        });
    });
    // script.js
    document.addEventListener("DOMContentLoaded", function() {
        // Select all section titles
        var sectionTitles = document.querySelectorAll("h2");

        // Add click event listener to each section title
        sectionTitles.forEach(function(title) {
            title.addEventListener("click", function() {
                // Toggle the display of the next sibling element (content under each section title)
                var nextElement = title.nextElementSibling;
                while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                    nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";


                    // script.js
                    document.addEventListener("DOMContentLoaded", function() {
                        // Select all section titles
                        var sectionTitles = document.querySelectorAll("h2");

                        // Add click event listener to each section title
                        sectionTitles.forEach(function(title) {
                            title.addEventListener("click", function() {
                                // Toggle the display of the next sibling element (content under each section title)
                                var nextElement = title.nextElementSibling;
                                while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                    nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                    nextElement = nextElement.nextElementSibling;
                                }
                            });
                        });
                    });
                    // script.js
                    document.addEventListener("DOMContentLoaded", function() {
                        // Select all section titles
                        var sectionTitles = document.querySelectorAll("h2");

                        // Add click event listener to each section title
                        sectionTitles.forEach(function(title) {
                            title.addEventListener("click", function() {
                                // Toggle the display of the next sibling element (content under each section title)
                                var nextElement = title.nextElementSibling;
                                while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                    nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                    nextElement = nextElement.nextElementSibling;
                                }
                            });
                        });
                    });
               
                  
                    }
                    // script.js
                    document.addEventListener("DOMContentLoaded", function() {
                        // Select all section titles
                        var sectionTitles = document.querySelectorAll("h2");

                        // Add click event listener to each section title
                        sectionTitles.forEach(function(title) {
                            title.addEventListener("click", function() {
                                // Toggle the display of the next sibling element (content under each section title)
                                var nextElement = title.nextElementSibling;
                                while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                    nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                    nextElement = nextElement.nextElementSibling;
                                }
                            });
                        });
                    });

                /* styles.css */
                body {
                    font-family: Arial, sans-serif;
                    margin: 0;
                    padding: 0;
                }

                #heading {
                    color: navy;
                    font-weight: bold;
                    text-align: center;
                    padding: 20px 0;
                }

                #content {
                    max-width: 800px;
                    margin: 0 auto;
                    padding: 20px;
                    line-height: 1.6;
                }

                ul {
                    margin: 20px 0;
                }

                li {
                    margin-bottom: 10px;
                }

                h2 {
                    color: navy;
                    font-weight: bold;
                }
                // script.js
                document.addEventListener("DOMContentLoaded", function() {
                    // Select all section titles
                    var sectionTitles = document.querySelectorAll("h2");

                    // Add click event listener to each section title
                    sectionTitles.forEach(function(title) {
                        title.addEventListener("click", function() {
                            // Toggle the display of the next sibling element (content under each section title)
                            var nextElement = title.nextElementSibling;
                            while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                nextElement =
                                    // script.js
                                    document.addEventListener("DOMContentLoaded", function() {
                                        // Select all section titles
                                        var sectionTitles = document.querySelectorAll("h2");

                                        // Add click event listener to each section title
                                        sectionTitles.forEach(function(title) {
                                            title.addEventListener("click", function() {
                                                // Toggle the display of the next sibling element (content under each section title)
                                                var nextElement = title.nextElementSibling;
                                                while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                                    nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                                    nextElement = nextElement.nextElementSibling;
                                                }
                                            });
                                        });
                                    });
                                // script.js
                                document.addEventListener("DOMContentLoaded", function() {
                                    // Select all section titles
                                    var sectionTitles = document.querySelectorAll("h2");

                                    // Add click event listener to each section title
                                    sectionTitles.forEach(function(title) {
                                        title.addEventListener("click", function() {
                                            // Toggle the display of the next sibling element (content under each section title)
                                            var nextElement = title.nextElementSibling;
                                            while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                                nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                                nextElement = nextElement.nextElementSibling;
                                            }
                                        });
                                    });
                                });
                                // script.js
                                document.addEventListener("DOMContentLoaded", function() {
                                    // Select all section titles
                                    var sectionTitles = document.querySelectorAll("h2");

                                    // Add click event listener to each section title
                                    sectionTitles.forEach(function(title) {
                                        title.addEventListener("click", function() {
                                            // Toggle the display of the next sibling element (content under each section title)
                                            var nextElement = title.nextElementSibling;
                                            while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                                nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                                nextElement = nextElement.nextElementSibling;
                                            }
                                        });
                                    });
                                });
                                // script.js
                                document.addEventListener("DOMContentLoaded", function() {
                                    // Select all section titles
                                    var sectionTitles = document.querySelectorAll("h2");

                                    // Add click event listener to each section title
                                    sectionTitles.forEach(function(title) {
                                        title.addEventListener("click", function() {
                                            // Toggle the display of the next sibling element (content under each section title)
                                            var nextElement = title.nextElementSibling;
                                            while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                                nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                                nextElement = nextElement.nextElementSibling;
                                            }
                                        });
                                    });
                                });
                                // script.js
                                document.addEventListener("DOMContentLoaded", function() {
                                    // Select all section titles
                                    var sectionTitles = document.querySelectorAll("h2");

                                    // Add click event listener to each section title
                                    sectionTitles.forEach(function(title) {
                                        title.addEventListener("click", function() {
                                            // Toggle the display of the next sibling element (content under each section title)
                                            var nextElement = title.nextElementSibling;
                                            while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                                nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                                nextElement = nextElement.nextElementSibling;
                                            }
                                        });
                                    });
                                });
                                // script.js
                                document.addEventListener("DOMContentLoaded", function() {
                                    // Select all section titles
                                    var sectionTitles = document.querySelectorAll("h2");

                                    // Add click event listener to each section title
                                    sectionTitles.forEach(function(title) {
                                        title.addEventListener("click", function() {
                                            // Toggle the display of the next sibling element (content under each section title)
                                            var nextElement = title.nextElementSibling;
                                            while (nextElement && (nextElement.tagName === 'P' || nextElement.tagName === 'UL')) {
                                                nextElement.style.display = nextElement.style.display === "none" ? "block" : "none";
                                                nextElement = nextElement.nextElementSibling;
                                            }
                                        });
                                    });
                                });
